// class Movie {
//     constructor(movie) { 
//       this.id = movie.id;
//       this.title = movie.title;
//       Movie.all.push(this);
//     }

//     static findById(id) {
//         return this.all.find(m => m.id === id);
//       }
// }

// Movie.all = [];